#!/bin/bash


# Find config
# location for weight storage CSV

# create graph using gnuplot 
# default records back == 10
# but can also go back further 
# can also do time, by filtering with EPOCHTIME
    # but can call this script with a number to show the weight graph (or sea
